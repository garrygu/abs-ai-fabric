# ABS Workstation Console – environment variables (Vite)
# Copy to .env and adjust as needed.

# Hub Gateway API base URL (assets, health, admin)
VITE_GATEWAY_URL=http://127.0.0.1:8081

# GPU Metrics server URL (host process serving nvidia-smi; started by core/start-core.ps1)
VITE_GPU_METRICS_URL=http://127.0.0.1:8083

# CES mode: enable CES-specific UI (banner, restrictions). Use false to disable.
# CES_MODE=false
