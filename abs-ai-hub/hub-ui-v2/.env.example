# Hub UI v2 – environment variables (Vite)
# Copy to .env and adjust as needed.
# See CES_MODE_SETUP.md for CES mode details.

# Gateway API base URL (assets, apps, admin, health)
VITE_GATEWAY_URL=http://localhost:8081

# CES mode: show CES-specific UI (banner, restrictions). Use true or 1 to enable.
VITE_CES_MODE=false

# Optional: enable CES mode debug logs in development only
# VITE_DEBUG_CES_MODE=false

# Build-time alternative (vite.config also reads this when building):
# CES_MODE=true
