# ABS Core – environment variables for docker-compose
# Copy to .env and adjust as needed.

# Docker network name (used by abs-net)
ABS_NETWORK=abs-net

# Timezone inside containers (ollama, qdrant, postgresql)
TZ=UTC

# Ollama host port (container listens on 11434)
OLLAMA_PORT=11434

# ---- Optional: only if you enable vLLM in docker-compose.yml ----
# VLLM_MODEL=meta-llama/Llama-2-7b-chat-hf
# VLLM_PORT=8000
