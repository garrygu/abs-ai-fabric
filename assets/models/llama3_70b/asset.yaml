asset_id: llama3:70b
display_name: LLaMA 3 70B
version: "1.0.0"

interface: llm-runtime
interface_version: v1
class: model

description: Meta LLaMA 3 70B parameter model - Secondary CES demo model for reliable language generation

runtime:
  type: external
  install:
    command: ollama pull llama3:70b
    once: true

lifecycle:
  desired: on-demand

resources:
  gpu_required: true
  min_vram_gb: 48
  min_ram_gb: 64
  disk_gb: 42

ownership:
  provider: system
  visibility: shared
  requestable: true

metadata:
  provider: ollama
  parameters: 70B
  format: GGUF Q4_K_M
  category: Language
  ces_fallback: true
