asset_id: deposition-summarizer
display_name: Deposition Summarizer
version: "1.0.0"

interface: application
interface_version: v1
class: app

description: Summarize deposition transcripts and extract key information

policy:
  required_models:
    - llama3.2:3b
  allowed_embeddings:
    - all-minilm
    - bge-small-en-v1.5
  defaults:
    embed_model: all-minilm
    provider: ollama
    dim: 384

lifecycle:
  desired: on-demand

ownership:
  provider: admin
  visibility: shared
  requestable: true

metadata:
  url: http://localhost:7860
  port: 7860
  category: Legal Apps
  tags:
    - deposition
    - summarization
    - transcript-analysis
    - legal-tech
  tier: abs
  status: active
